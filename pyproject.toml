[project]
name = "reviewmind"
version = "0.1.0"
description = "AI-powered code review for GitHub pull requests"
requires-python = ">=3.12"

[tool.black]
line-length = 88
target-version = ["py312"]

[tool.isort]
profile = "black"
line_length = 88

